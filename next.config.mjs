/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Ensures static export
//   basePath: "/web-app", // Set repo name as base path
//   assetPrefix: "/web-app/",
};

export default nextConfig;
